// import { decorate, observable, action, computed, toJS } from 'mobx';

// class generalStore {

// }
// decorate(publicStore, {
//   userId: observable,
//   token: observable,
//   tokenExpiration: observable,
// })

// export default new generalStore();